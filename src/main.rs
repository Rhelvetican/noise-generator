use image::RgbImage;
use std::{
    env,
    str,
    path::PathBuf
};
use rand::Rng;
use noise_generator::*;

const USAGE: &'static str = "Usage: ./noisegen [-arguments] <output location>";

fn main() -> Result<(), ()> {
    

    println!("Hello, world!");

    Ok(())
}
